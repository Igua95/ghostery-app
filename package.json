{
  "name": "ghostery-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "docker-compose up --build",
    "start": "docker-compose up -d",
    "stop": "docker-compose down",
    "setup": "./run-project.sh"
  },
  "dependencies": {
    "typescript": "^4.5.4",
    "eslint": "^7.32.0",
    "prettier": "^2.5.1"
  },
  "devDependencies": {
    "docker-compose": "^1.29.2"
  }
}